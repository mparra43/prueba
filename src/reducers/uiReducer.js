import { types } from "../types/types";

const initialState = {

}



export const uiReducer = ( state = initialState, action ) => {

    switch ( action.type ) {

    
        default:
            return state;
    }


}