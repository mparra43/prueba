import {types} from '../types/types';


const initialState = {


}

export const categoriesReducer = (state = initialState, action) => {


    switch (action.type) {


        default:
            return state;
    }

}
